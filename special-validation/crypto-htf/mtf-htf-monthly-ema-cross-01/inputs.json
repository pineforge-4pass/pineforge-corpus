{
  "_comment": "ETH-USDT-USDT 15m warmup6m (corpus default feed). Probe currently BLOCKED at engine runtime validation: request.security with calendar-monthly timeframe 'M' is rejected by validate_security_timeframes (engine_security.cpp:140, 'invalid timeframe literal M') even though the CALENDAR-month TimeframeAggregator path is wired (register_security_eval handles tf_ratio==-1) and calendar_period_for maps 'M'->MONTH. Transpiled + compiled OK; run produced no engine_trades.csv. Becomes a ready parity test once monthly request.security is accepted by the validation layer."
}
